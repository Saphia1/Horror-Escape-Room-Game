""" 
import pygame as py
from Screens import Screens
from gameloop import maingame
from inventory import inventory """

""" class displayinventory(Screens):
    def __init__(self,bgcolour=(70, 72, 74)):
        super().__init__(bgcolour)
        self._text=self.getinventory()
        value["Text"],value["Textcoords"][0],value["Textcoords"][1], value["Textsize"],value["Textcolour"]
    

    def Update(self):
        running=True
        WIDTH=500
        HEIGHT=500
        self._screen=py.display.set_mode([WIDTH,HEIGHT])
        py.event.get()
        while running:
            self._screen.fill((158, 70, 80))
            self,text, x , y, fontsize, fontcolour=None
            self.getinventory
            for key, value in self._playbutton.items()
                self.draw_text()
  
            py.display.flip()
            self._clock.tick(30)
py.quit() """


""" 
displayinventory().Update()
#    """

   
      
    
